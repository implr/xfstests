# Heavy fsstress / heavy-write soaks. Two failure modes seen on this kernel
# + ZFS 2.4.1, both unrecoverable without reboot:
#   1. Rangelock livelock (013): fsstress writers hold per-vnode rangelocks
#      while zil_commit, called from the same writer's fsync/sync_fs path,
#      blocks in zfs_get_data -> zfs_rangelock_enter waiting on that same
#      writer's rangelock. Threads pile up in D state (cv_wait_common ->
#      zfs_rangelock_enter_impl -> zfs_get_data -> zil_lwb_write_issue ->
#      zil_commit_impl -> zfs_sync); even cleanup rm -rf wedges in
#      vfs_unlink. SIGKILL leaves rangelocks pinned.
#   2. Directory-op concurrency wedge (310): parallel read/readdir/lseek on
#      the same directory fd; ZFS appears to contend on the directory ZAP
#      lock indefinitely. Test never terminates and leaves D-state
#      kernel threads.
#   3. ARC OOM-kernel-panic (083/269/476/650): the in-flight write
#      backlog plus dirty data outpaces the ARC reclaim path on a 4 GiB
#      scratch pool; the kernel OOM-kills its way through the whole VM
#      before ZFS releases dirty buffers. Confirmed: running 083+269+476+650
#      together kernel-panicked the VM.
# All five tests fall in the same ZFS write-stress family. Defer until ZFS
# tunables (zfs_dirty_data_max, ARC limits) are configured for this VM size,
# or until openzfs fixes the rangelock/ZIL contention.
# Upstream:
#   openzfs/zfs#11792 - deadlock when running xfstests generic/013 (open) -
#     direct match for the rangelock livelock above.
#   openzfs/zfs#18050 - multiple OOM killings on speedy rsync backup (open) -
#     ARC reclaim can't keep up with dirty-data pressure; same family as the
#     083/269/476/650 panic.
generic/013
generic/083
generic/269
generic/310
generic/476
generic/650

# NixOS test VM: same ZFS reflink + cycle_mount / DIO race family as the
# heavy-fsstress bucket above, but exposed reliably under the tighter
# timing of the qemu-kvm + 9p test runner. They pass on plain Ubuntu in
# our setup. Not actionable from xfstests' side; either openzfs upstream
# fixes the underlying rangelock/ZIL races, or we tune zfs_dirty_data_max
# / vdev_async_write_active_max_dirty_percent for the runner.
#   118 - reflink + buffered write + _test_cycle_mount → umount busy
#   119 - same family
#   139 - directio CoW + _test_cycle_mount → file2 md5 mismatch
#   175 - sub-recordsize reflink path that hits XFS_IOC_CLONE_RANGE EINVAL
#         less reliably on Ubuntu, more reliably on the NixOS runner
#   551 - aio-dio-write-verify times out under tighter scheduling
#   591 - splice + concurrent reader/writer races
generic/118
generic/119
generic/139
generic/175
generic/551
generic/591

# ZFS doesn't track setuid/setgid/fscaps the way other filesystems do —
# clears too aggressively in some cases, not at all in others. Same class as
# the POSIX-ACL quirks below; candidate for openzfs upstream reports.
#   093 - append doesn't clear fscaps
#   193 - chown(2) clears sgid bit even when group-exec was not set
#   355 - direct write doesn't clear suid/sgid
#   375 - chmod doesn't clear SGID in some cases
#   513 - reflink doesn't clear fscaps
#   673 - reflink doesn't clear suid/sgid
#   675 - reflink doesn't clear fscaps
#   683 - fallocate doesn't clear suid/sgid
#   684 - fpunch doesn't clear suid/sgid
#   685 - fzero doesn't clear suid/sgid
#   688 - fallocate doesn't clear fscaps
# (683/684/685/688 were auto-notrun before recordsize=64K via alloc-unit check.)
generic/093
generic/193
generic/355
generic/375
generic/513
generic/673
generic/675
generic/683
generic/684
generic/685
generic/688

# ZFS POSIX-ACL (acltype=posix) quirks:
#   319 - parent default ACL's o:: perms not intersected with requested mode;
#         child gets other::r-x where expected other::---
#   444 - parent dir's SGID bit not inherited by child mkdir when default ACL
#         is present
#   449 - acl+attr ENOSPC test: file mode bits change to "-rwxr-----" when
#         setfacl partially applies; xattr-stored ACL on ZFS leaks group bits
generic/319
generic/444
generic/449

# ZFS fallocate is a no-op: returns success but doesn't reserve blocks.
# Tests that depend on real prealloc semantics therefore fail.
# Upstream openzfs/zfs#11073 (closed) acknowledged the advertise-but-don't-
# allocate behavior; allocation was never actually implemented.
#   103 - allocates blocks on nearly-full fs, expects success
#   228 - tests prealloc hitting RLIMIT_FSIZE
#   256 - dir prealloc (mkfs_sized + fallocate on dir)
#   274 - write into preallocated region on nearly-full fs
#   422 - falloc -k then pwrite into preallocated region; compares du blocks
#         before/after writeback. Without real prealloc, the "before" count
#         doesn't include the falloc range, so it diverges from "after".
#   586 - expects extsize-style prealloc past write end
generic/103
generic/228
generic/256
generic/274
generic/422
generic/586

# Tight-pool ENOSPC vs near-full slack: with a sized-loop-backed pool, ZFS
# pool overhead leaves so little headroom that even open()/touch can hit
# ENOSPC where the test expects a partial-write-then-ENOSPC. Refquota used
# to leave more slack and these passed; the ENOSPC is now correct but the
# tests' expectations don't match ZFS's pool-level accounting.
generic/275
generic/387
generic/511

# Tests that stack another block layer (LVM, dm-error, dm-thin, dmhugedisk)
# on top of the scratch device. Doesn't work when the scratch device is
# owned by an imported zpool — would need ZFS-aware setup helpers that
# export the pool, build the dm/LVM stack, then re-create the pool on top.
# Several sub-categories with different mechanisms but the same root cause:
#   LVM (pvcreate/vgcreate/lvcreate, including dm-snapshot via lvcreate):
#     081, 108
#   dm-error CoW failure-injection (most are reflink + dm-error variants;
#     dm-error setup is what blocks them, not the reflink path):
#     250, 252, 265-268, 271-272, 276, 278-279, 281-283, 329, 331, 338,
#     441, 475, 484, 648, 753
#   dm-thin (would auto-notrun via _require_dm_target on kernels without
#     dm-thin loaded; we keep them explicit since modprobe usually succeeds):
#     347, 405
#   dmhugedisk (likewise auto-notrun via _require_dmhugedisk on kernels
#     without it):
#     620
generic/081
generic/108
generic/250
generic/252
generic/265
generic/266
generic/267
generic/268
generic/271
generic/272
generic/276
generic/278
generic/279
generic/281
generic/282
generic/283
generic/329
generic/331
generic/338
generic/347
generic/405
generic/441
generic/475
generic/484
generic/620
generic/648
generic/753

# Mount-shared-subtree tests that bypass _scratch_mount and call
# `mount -t $FSTYP $SCRATCH_DEV ...` directly on the block device. ZFS
# expects the dataset name as the mount source, not the device, so every
# such mount fails with "mount: unknown filesystem". Different mechanism
# from the dm/LVM bucket above but same family of "tests don't go through
# the FS-specific mount helper". Would need either a ZFS branch in each
# test or a generic _get_mount that respects FSTYP=zfs.
generic/409
generic/410
generic/411
generic/589

# Delayed-allocation ENOSPC test: pre-existing exclusion from the initial
# ZFS port (Behlendorf's old patches). ZFS uses txg-based commits rather
# than page-based delayed allocation, so the test's premise (silent dmesg
# under heavy concurrent buffered write + O_DIRECT read at near-ENOSPC)
# doesn't map cleanly. Root cause not re-characterized on the current
# branch; preserved as-is.
generic/224

# Test creates a FS image inside SCRATCH_MNT, creates a loop device for it,
# then calls _mkfs_dev on the loop dev — which for us becomes `zpool create
# /dev/loopN` and fails ("invalid character '/' in pool name"). Nested-
# zpool-on-loop isn't supported by our helpers; structural.
generic/361

# fsx soaks that trip ZFS' DIO/buffered/io_uring CoW races. 091 fails fsx
# READ BAD DATA only when recordsize < 128K (we run with 64K). 263 is the
# DIO+sub-block-buffered fsx variant. 616/617 are the io_uring buffered/DIO
# fsx soaks. 759/760 are the newer fsx soak variants (every falloc mode ZFS
# lacks gets auto-disabled, then the CoW/DIO soak still trips). Same family
# as 363. Needs ZFS-side investigation.
generic/091
generic/263
generic/616
generic/617
generic/759
generic/760

# https://github.com/openzfs/zfs/issues/16014 — high-offset reflink: cp
# --reflink=always on a sparse file gets SIGKILL'd, test output truncates
# after "Reflink large empty file". Confirmed still failing 2026-04-25.
generic/303

# Sub-recordsize reflink/copy_range issues:
#   144 - reflinks blksz*4+37 bytes; ZFS rejects the sub-recordsize tail with
#         EINVAL since block_cloning only clones whole recordsize records
#   564 - the _require_xfs_io_command "copy_range" "-f" probe in common/rc
#         runs `pwrite 0 4k` then `copy_range -f 0 -d 4k -l 4k` against the
#         same file (self-overlap, sub-recordsize). ZFS hangs in
#         zfs_rangelock_enter waiting on a rangelock the same fd already
#         holds — uninterruptible D-state, only freed by reboot. Real ZFS
#         deadlock; candidate for openzfs upstream report.
generic/144
generic/564

# fsx -e 1 post-EOF-page pollution: tests a zeroing behavior that's
# fs-specific (opt-in per commit 9e2b0f4c). Also fails on tmpfs, not a
# ZFS correctness issue.
generic/363

# ZFS doesn't implement cgroup-aware writeback accounting (same as most
# non-btrfs/non-xfs filesystems). Tracked upstream as openzfs/zfs#13662
# "Annotate writeback I/O to the correct cgroup" (open).
generic/563

# open_by_handle on a kept-open-but-unlinked file: ZFS returns ESTALE (116)
# after drop_caches, where ext4/xfs/btrfs return success. ZFS keeps unlinked-
# but-referenced inodes on its on-disk unlinked set, but fh_to_dentry doesn't
# resolve through that set after the inode is evicted from cache. Real ZFS
# bug, candidate for openzfs upstream report.
generic/467

# t_readdir_3 random-offset seek + getdents64 returns 0 (EOF) on ZFS. ZFS
# readdir d_off values are ZAP hash cookies, not a dense monotonic space —
# a random offset in [0, maxpos] often lands between hash buckets where no
# entry exists. Test was written for the udf seekdir crash (a48fc69fe658)
# and assumes random offsets always yield an entry; that assumption is
# fs-specific and incompatible with hash-cookie directories like ZFS.
generic/676

# Reflink + mmap CoW on a full filesystem: after filling the fs to ENOSPC,
# the test expects the next mmap CoW store to fault SIGBUS (out of space to
# break the shared extent). ZFS returns cleanly instead (no SIGBUS), so the
# test's SIGBUS handler never fires. Same reflink/CoW space-accounting family
# as the sub-recordsize reflink bucket; ZFS doesn't reserve-on-fault the way
# the test assumes.
generic/173

# idmapped-mounts nested user namespace: vfstest/idmapped-mounts'
# nested_userns case fails at sys_mount_setattr when re-mapping an already
# idmapped mount from within a nested userns. ZFS idmapped-mount support
# doesn't cover the nested-userns remap path. Structural ZFS/VFS gap.
generic/645

# st_blocks accounting after truncate: test extends a file (or punches) and
# checks the allocated block count stays within a tight range, but ZFS
# reports st_blocks=1 — ZFS is sparse and txg-delayed, so blocks aren't
# accounted the way the test expects (same falloc-is-a-no-op / delayed-alloc
# family as the other space-accounting excludes).
generic/701

# DIO + buffered mid-write checksum race: an io thread fails during a
# concurrent direct/buffered write mix. Reproducible; likely a real ZFS
# DIO coherency bug rather than a test/harness issue. Candidate for an
# openzfs upstream report; excluded for now with the rest.
generic/761
